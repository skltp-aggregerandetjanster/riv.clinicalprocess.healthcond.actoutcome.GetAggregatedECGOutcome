# Mule Application Deployment Descriptor
redeployment.enabled=true
encoding=UTF-8

# Only load GetAggregatedECGOutcome-common.xml, it will import the other config files since mule-deploy.properties can't load config-files from jar-files on the classpath, e.g. agp-core.jar
# config.resources=GetAggregatedECGOutcome-common.xml,aggregating-services-common.xml,aggregating-service.xml,process-notification-service.xml
config.resources=GetAggregatedECGOutcome-common.xml

domain=default
